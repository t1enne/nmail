#!/bin/bash
# $1 = "mail:error", $2 = queue-id, $3 = error message
id="${2:-unknown}"
err="${3:-unknown error}"

if command -v notify-send &>/dev/null; then
    notify-send -u critical "❌ Mail Send Failed" "$id: $err" \
        --icon=dialog-error \
        --category=email
fi
