#!/bin/bash
# $1 = "mail:new", $2 = count
count="${2:-0}"
if (( count > 0 )) && command -v notify-send &>/dev/null; then
    notify-send "📬 New Mail" "$count new message(s)" \
        --icon=mail-unread \
        --category=email \
        --hint=int:transient:1
fi
